# --- Databases ---
PROD_DB_URL=postgresql+psycopg://user:pass@prod-host:5432/production
REPORT_DB_URL=postgresql+psycopg://user:pass@report-host:5432/report
OPSLOG_DB_URL=postgresql+psycopg://opslog:change-me@localhost:5432/opslog

# --- Run context ---
# RUN_ID=<uuid>        # optional
RUN_TRIGGER=cron
RUN_SOURCE=k8s

# --- Notifications (optional) ---
# NOTIFY_EMAIL_TO=ops@example.com,dev@example.com
# NOTIFY_EMAIL_FROM=noreply@example.com
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=smtp-user
# SMTP_PASSWORD=secret
# SMTP_USE_TLS=true
