apiVersion: v1
kind: Secret
metadata:
  name: core-runner-secrets
type: Opaque
stringData:
  PROD_DB_URL: "postgresql+psycopg://user:pass@prod-svc:5432/production"
  REPORT_DB_URL: "postgresql+psycopg://user:pass@report-svc:5432/report"
  OPSLOG_DB_URL: "postgresql+psycopg://opslog:strongpass@opslog-svc:5432/opslog"
  # NOTIFY_EMAIL_TO: "ops@example.com"
  # NOTIFY_EMAIL_FROM: "noreply@example.com"
  # SMTP_HOST: "smtp.example.com"
  # SMTP_PORT: "587"
  # SMTP_USER: "smtp-user"
  # SMTP_PASSWORD: "secret"
  # SMTP_USE_TLS: "true"
